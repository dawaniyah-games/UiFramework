<?xml version="1.0" encoding="utf-8"?>
<engine:UIDocument
    xmlns:engine="UnityEngine.UIElements"
    xmlns:uie="UnityEditor.UIElements"
    xmlns="UnityEngine.UIElements">

  <Style src="EditorWindow.uss" />

  <VisualElement name="MainContainer" style="flex-grow: 1; padding: 10px;">

    <!-- Title -->
    <Label text="🎛️ UI Framework Setup Manager" class="title" />

    <!-- Config Path -->
    <VisualElement class="config-box">
      <Label text="UI Config Path:" />
      <TextField name="ConfigPathField" binding-path="UiSetupAssetPath" />
    </VisualElement>

    <!-- Buttons -->
    <VisualElement class="button-row">
      <Button name="LoadButton" text="🔄 Load Setup" />
      <Button name="SaveButton" text="💾 Save Setup" />
      <Button name="RevertButton" text="↩ Revert" />
    </VisualElement>

    <!-- Divider -->
    <VisualElement style="height: 2px; background-color: #666666; margin: 12px 0;" />

    <!-- Scrollable content list -->
    <ScrollView name="StateList" />

    <!-- Bottom Button -->
    <VisualElement class="bottom-row">
      <Button name="AddStateButton" text="➕ Add New State" />
    </VisualElement>

  </VisualElement>
</engine:UIDocument>

<!--
  This UXML file defines the layout for the UI Framework Setup Manager Editor Window.
  It includes a title, a field for the UI config path, buttons for loading, saving,
  and reverting setup, a scrollable list for states, and a button to add new states.
  The styles are defined in EditorWindow.uss.
  The main container uses flexbox for layout, and the elements are styled for clarity.
  The buttons are labeled with emojis for better visual distinction.
  The scrollable content list will be populated dynamically with state entries.
  The bottom row contains a button to add new states.
  The divider visually separates the config section from the state list.
  The config box and button row are styled for better user experience.
  The UXML file is designed to be clean and user-friendly, making it easy to manage UI setups.
--> 